plugins {
	id 'java'
	id 'org.springframework.boot' version '3.4.2'
	id 'io.spring.dependency-management' version '1.1.7'
	id 'org.sonarqube' version '5.0.0.4638'
}

group = 'com.heitor'
version = '0.0.1-SNAPSHOT'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(25)
	}
}

configurations {
	compileOnly {
		extendsFrom annotationProcessor
	}
}

repositories {
	mavenCentral()
}

dependencies {
	// Spring Boot Core
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-web'
	implementation 'org.springframework.kafka:spring-kafka'

	// Lombok - Versão estável com suporte ao compilador do Java 25
	compileOnly 'org.projectlombok:lombok:1.18.42'
	annotationProcessor 'org.projectlombok:lombok:1.18.42'
	testCompileOnly 'org.projectlombok:lombok:1.18.42'
	testAnnotationProcessor 'org.projectlombok:lombok:1.18.42'

	// MapStruct original preservado
	implementation 'org.mapstruct:mapstruct:1.6.3'
	annotationProcessor 'org.mapstruct:mapstruct-processor:1.6.3'

	// Banco de dados e infraestrutura de testes padrão
	runtimeOnly 'com.h2database:h2'
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	testImplementation 'org.springframework.kafka:spring-kafka-test'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'

	// CORREÇÃO DEFINITIVA DO MOCKITO NO JAVA 25:
	// Força a versão 1.17.5+ do ByteBuddy que possui o leitor de bytecode 69 (Java 25)
	testImplementation 'org.mockito:mockito-core:5.14.2'
	testImplementation 'org.mockito:mockito-junit-jupiter:5.14.2'
	testImplementation 'net.bytebuddy:byte-buddy:1.17.5'
	testImplementation 'net.bytebuddy:byte-buddy-agent:1.17.5'
}

tasks.named('test') {
	useJUnitPlatform()
	// Força a JVM de teste a habilitar suporte experimental para assinaturas do Java 25
	systemProperty 'net.bytebuddy.experimental', 'true'
}

springBoot {
	mainClass = 'com.heitor.checkingaccountoperation.CheckingAccountOperationApplication'
}

sonar {
	properties {
		property "sonar.host.url", "https://sonarcloud.io"
	}
}
