name: SonarQube Analysis

on:
  push:
    branches:
      - main
      - master
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonar:
    name: Build and Analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Instalamos o Java 24 para que o Gradle 9.x consiga rodar sem estourar o erro de JVM maxima suportada
      - name: Set up JDK 24 for Gradle Daemon
        uses: actions/setup-java@v4
        with:
          java-version: '24'
          distribution: 'temurin'
          cache: gradle

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      # Removemos propriedades locais conflitantes geradas no Windows
      - name: Remove local Windows Java Home
        run: |
          if [ -f gradle.properties ]; then
            echo "Limpando configuracoes locais de Java Home do Windows..."
            sed -i '/org.gradle.java.home/d' gradle.properties
          fi

      # Força o Gradle a baixar automaticamente o JDK 25 quando for compilar o Toolchain do seu projeto
      - name: Enable Gradle Toolchain Auto-Provisioning
        run: |
          mkdir -p ~/.gradle
          echo "org.gradle.java.installations.auto-download=true" >> ~/.gradle/gradle.properties

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build and analyze
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./gradlew build sonar \
            -Dsonar.projectKey=HeitorSeemann_checking-account-operation \
            -Dsonar.organization=HeitorSeemann \
            -Dsonar.host.url=https://sonarcloud.io
